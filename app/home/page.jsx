// Re-export the preview marketing page at the new /home route
export { default } from "../preview/page";

return (
  // removed gradient classes, using transparent bg instead
  <div className="bg-transparent">
    {/* ...existing code... */}
  </div>
);
